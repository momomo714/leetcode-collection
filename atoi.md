题目：字符串转换整数

题目要求比较多，给出需要实现的关键点：

1\.忽略前导空格

2\.处理可选的正负号

3\.连续读取数字直到非数字字符

4\.处理溢出

5\.没有数字则返回0

思路：对于以上各个问题的解答

1\.使用i遍历，遇到空格直接跳过

2\.跳过空格后，检查当前字符是否为正负

3\.继续循环，result = result \* 10 + digit

4\.运用INT\_MAX和INT\_MIN判断是否溢出

5\.特殊情况：全是空格/没有数字，返回0

