思路：通过二分法在两个数组所有元素中划线，使得左边元素个数为（m+n+1）/2，并且左边所有元素<=右边所有元素。中位数由分割线处元素确定。

实现方法：（1）在较短的数组上二分搜索，确定一个i为该数组的左侧元素个数

（2）则另一个数组左侧元素个数为j=(m+n+1)/2-i

（3）通过调整i使得nums1\[i-1] ≤ nums2\[j] \&\& nums2\[j-1] ≤ nums1\[i]

（4）找到i后，通过总长度奇偶性返回中位数

