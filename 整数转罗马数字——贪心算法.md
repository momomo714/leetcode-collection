题目：整数转罗马数字
贪心策略：每一步都选择当前尽可能大的罗马数字符号，直到不能加为止
思路：
1.定义两个数组：values,symbols，且初始化结果字符串result为空
2.遍历values数组：对于当前数值values[i],只要num大于等于它，就执行：
（1）num-=values[i];
（2）result+=symbols[i];
重复这个步骤，直到num小于values[i],然后进入下一个步骤
3.返回result
